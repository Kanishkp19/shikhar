import { SettingsClient } from "@/components/settings/settings-client";

/**
 * Settings page — push notifications enable/disable + plan overview.
 */
export default async function SettingsPage() {
  return <SettingsClient />;
}
